<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.david.webapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".personal" />
        <activity
            android:name=".education"
            android:label="@string/title_activity_education"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".employement"
            android:label="@string/title_activity_employement"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".interet"
            android:label="@string/title_activity_interet"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".about" />
        <activity android:name=".ref" />
        <activity android:name=".project"></activity>
    </application>

</manifest>